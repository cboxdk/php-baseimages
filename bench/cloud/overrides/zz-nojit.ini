; Pass-2 experiment: isolate the JIT effect on Laravel (SSU ships JIT off and
; wins the lv slots; tracing JIT is a known wash-or-loss on big OOP codebases).
opcache.jit = disable
opcache.jit_buffer_size = 0
