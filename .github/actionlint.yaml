# actionlint configuration.
# Declare the custom self-hosted runner labels so actionlint doesn't flag them
# as unknown. These match the labels on the org's self-hosted build runners.
self-hosted-runner:
  labels:
    - buildx
