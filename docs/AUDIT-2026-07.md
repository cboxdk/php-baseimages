# Base image audit — 2026-07 (Kubernetes / Traefik / Statamic hardening)

Four-dimension audit (nginx/proxy, entrypoint shell, cbox-init lifecycle, Laravel/Statamic fit)
prompted by an nginx-behind-Traefik bug where the app saw `http` and generated `http://` URLs
(and Statamic 404'd on request/entry URL scheme mismatch).

## Fixed in this change (php-fpm-nginx layer + shared config)

| # | Area | Fix |
|---|---|---|
| 1 | **Forwarded scheme/port/HTTPS** | `map`s derive `$forwarded_proto`/`$forwarded_port`/`$forwarded_https` from the upstream `X-Forwarded-*` (fallback to `$scheme`/`$server_port`). `fastcgi_param HTTP_X_FORWARDED_PROTO/PORT` now honor the proxy; `fastcgi_param HTTPS` is finally set. Fixes `http://` URLs + wrong `$_SERVER['HTTPS']` behind a TLS-terminating proxy. |
| 2 | **`NGINX_REAL_IP_CONFIG` literal `\n`** | now `printf '%b'`-converted like the security headers → valid `set_real_ip_from` directives (previously "unknown directive" crash when `NGINX_TRUSTED_PROXIES` set). |
| 3 | **Security headers dropped on static assets** | re-emit `${NGINX_SECURITY_HEADERS}` inside the static-file location (nginx `add_header` doesn't inherit once a nested block adds one). |
| 4 | **`SSL_MODE=full` redirect loop behind a proxy** | http→https redirect gated on `$forwarded_proto = http` (no loop when TLS terminated upstream). |
| 5 | **`/health` localhost-only → k8s httpGet 403** | made public (static string, no info leak) so kubelet probes work. |
| 6 | **Empty mounted `storage/` volume** | entrypoint now `mkdir -p`s `storage/framework/{cache,sessions,views}`, `storage/logs`, `bootstrap/cache` before chown. |
| 7 | **Auto-migrate races across replicas** | `artisan migrate` now uses `--isolated` (atomic cache lock; only one replica migrates). |
| 8 | **Dead custom-ini path** | removed the `/etc/php/${PHP_VERSION}/fpm/conf.d/...` no-op (source builds use `/usr/local/etc/php`). |
| 9 | **Deprecated `listen ... http2`** | `listen ssl; http2 on;`. |
| 11 | **`client_max_body_size` mismatch** | http-level aligned to 100M (matched the per-server default). |
| 12 | **HEALTHCHECK start-period** | 10s → 30s (migrate/optimize can exceed 10s). |

## Addressed by cbox-init v2.2.0 (separate release, bumped in versions.json)

The pinned binary was **v2.1.1**; `main` was 5 unreleased commits ahead. Released **v2.2.0**, which
carries the process-manager–level findings:
- **Health readiness semantics** (a wedged/half-ready process no longer reports green).
- **Hardened process lifecycle** (start/stop/restart, signal/shutdown correctness).
- **Secure runtime observability defaults** (`:9090` metrics exposure).
- Plus dependency-aware reload + startup performance controls.
- Bonus: fixed `version` always reporting `1.0.0` (was a `const` the `-ldflags -X` couldn't override).

## Fixed in the second pass

| Area | Fix |
|---|---|
| **`chown -R $WORKDIR`** | skips the recursive chown when the workdir is already owned by the target user (avoids O(files) on every start over large mounted volumes); dropped the redundant per-dir loop. |
| **Framework auto-detect** | Laravel now requires `artisan` **and** (`laravel/framework` in composer.json or `bootstrap/app.php`) — no false positives from a stray `artisan` file. |
| **process_control_timeout** | added via a `[global]` include file (`fpm-global.conf` → `zz-global.conf`) — graceful drain now works (a pool file can't hold this directive). |
| **cbox-init.yaml nginx health port** | entrypoint keeps the health-check URL in sync with `NGINX_HTTP_PORT` (no restart-loop when the port is overridden). |
| **cbox-init.yaml sed injection** | all env values injected into the YAML are now sed-escaped (previously only `api_auth`). |
| **Read-only rootfs** | generated PHP ini/pool writes no longer abort the entrypoint on EROFS — they `log_warn` and continue. |
| **PROXY protocol** | new `NGINX_PROXY_PROTOCOL=on` adds `proxy_protocol` to the listen sockets + sets `real_ip_header proxy_protocol` (L4 proxies / HAProxy / NLB). |
| **`open_basedir`** | documented the `PHP_OPEN_BASEDIR` override for symlink-release deploys (kept the secure default). |
| **Docs** | `LARAVEL_SCHEDULER` correctly described as `schedule:work` + single-replica caveat. |

## Deferred (documented, lower risk / binary-coupled)

- `sanitize_nginx_value()` for the templated `NGINX_*` values — the generated config is already gated
  by cbox-init's `nginx -t` at startup (fails loud), and a blanket strip would corrupt legitimate
  values (CSP contains `;`, try_files contains `$`). Left as-is behind the validation gate.
- `printf '%b'` for the security-headers/mtls blocks can mangle a backslash in a header value — rare,
  and caught by the `nginx -t` gate. Rebuild those with real newlines in a focused pass.
- cbox-init.yaml php-fpm health is a TCP connect (a wedged FPM stays green) and worker
  `restart: always` — both are process-manager behavior largely covered by v2.2.0's health-readiness
  + lifecycle hardening; `request_terminate_timeout=60s` bounds a stuck request. Revisit against the
  new binary rather than papering over it in YAML (`restart: on-failure` would wrongly stop a
  `queue:work --max-time` worker that exits 0 by design).
- Docs: full read-only-rootfs tmpfs list, k8s manifests/probe guide, single-writer guidance for
  Statamic Git Automation (architecture note below).

## Health / readiness redesign (base image must not shadow app routes)

Same anti-pattern as the `X-Forwarded-Proto` bug: the base image was claiming an **app-facing
path**. nginx served a static `/health`, so the application's own `/health` (Laravel health,
spatie/laravel-health, etc.) was unreachable.

New model — the supervisor owns readiness:

- **cbox-init writes `/tmp/cbox-ready`** when *all* tracked processes are healthy (nginx + php-fpm +
  any enabled workers) and removes it otherwise. Enabled via `global.readiness` in the shipped
  `cbox-init.yaml` (the v2.2.0 binary already implements the manager). This is the authoritative
  "are we ready" signal because cbox-init supervises everything.
- **Kubernetes probes target the supervisor**, not nginx. cbox-init **v2.3.0** exposes ACTIVE HTTP
  endpoints (`global.readiness.http_port: 9091`), preferred over the passive file because a wedged
  supervisor fails the probe (a stale file cannot):
  `readinessProbe: { httpGet: { path: /readyz, port: 9091 } }` (200 only when all processes ready)
  and `livenessProbe: { httpGet: { path: /livez, port: 9091 } }` (supervisor responsive).
  The `/tmp/cbox-ready` file remains for `exec` probes.
- **nginx no longer serves `/health`** — it falls through to the app. `/health` and `/up` belong to
  the application.
- nginx keeps a tiny **localhost-only `/healthz`** used solely by cbox-init's internal nginx-process
  health check and the container `HEALTHCHECK` — never exposed, never collides with app routes.

Principle going forward: the base image serves the app and otherwise keeps its hands off the app's
path/namespace and request semantics (proto, host, port, health).

## Statamic Git Automation (architecture note)

Do **not** bake `content/` into the immutable image or enable CP git-push on a multi-replica web
Deployment. Mount `content/`, `public/assets`, `storage/forms` as volumes; run the editing CP on a
single writer (with the git credential), serve public traffic from read-only replicas. Migrations
via a Job; scheduler in exactly one place. The base image supplies no git identity/credentials.
