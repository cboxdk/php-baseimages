<?xml version="1.0" encoding="UTF-8"?>
<!--
  PHPeek Base Image - ImageMagick Security Policy
  Prevents common ImageMagick vulnerabilities:
  - SVG XXE attacks
  - PDF ghostscript vulnerabilities
  - Billion laughs attacks
  - SSRF via URL handlers
-->
<policymap>
  <!-- Disable risky coders that can be exploited -->
  <policy domain="coder" rights="none" pattern="EPHEMERAL" />
  <policy domain="coder" rights="none" pattern="URL" />
  <policy domain="coder" rights="none" pattern="HTTPS" />
  <policy domain="coder" rights="none" pattern="HTTP" />
  <policy domain="coder" rights="none" pattern="MVG" />
  <policy domain="coder" rights="none" pattern="MSL" />
  <policy domain="coder" rights="none" pattern="TEXT" />
  <policy domain="coder" rights="none" pattern="SHOW" />
  <policy domain="coder" rights="none" pattern="WIN" />
  <policy domain="coder" rights="none" pattern="PLT" />

  <!-- Disable PostScript formats (keep disabled for security) -->
  <policy domain="coder" rights="none" pattern="PS" />
  <policy domain="coder" rights="none" pattern="PS2" />
  <policy domain="coder" rights="none" pattern="PS3" />
  <policy domain="coder" rights="none" pattern="EPS" />
  <policy domain="coder" rights="none" pattern="XPS" />

  <!-- Enable PDF with read/write (Ghostscript is required and installed) -->
  <!-- NOTE: Users should validate PDF sources before processing -->
  <policy domain="coder" rights="read|write" pattern="PDF" />

  <!-- Enable SVG with read-only (write disabled for XXE prevention) -->
  <policy domain="coder" rights="read" pattern="SVG" />
  <policy domain="coder" rights="read" pattern="SVGZ" />

  <!-- Enable HEIC/HEIF formats -->
  <policy domain="coder" rights="read|write" pattern="HEIC" />
  <policy domain="coder" rights="read|write" pattern="HEIF" />

  <!-- Disable potentially dangerous delegates (except gs for PDF) -->
  <policy domain="delegate" rights="none" pattern="*" />

  <!-- Prevent indirect reads (file inclusion) -->
  <policy domain="path" rights="none" pattern="@*" />

  <!-- Resource limits to prevent DoS -->
  <policy domain="resource" name="memory" value="256MiB"/>
  <policy domain="resource" name="map" value="512MiB"/>
  <policy domain="resource" name="width" value="16KP"/>
  <policy domain="resource" name="height" value="16KP"/>
  <policy domain="resource" name="area" value="128MP"/>
  <policy domain="resource" name="disk" value="1GiB"/>
  <policy domain="resource" name="file" value="768"/>
  <policy domain="resource" name="thread" value="4"/>
  <policy domain="resource" name="throttle" value="0"/>
  <policy domain="resource" name="time" value="120"/>
  <policy domain="resource" name="list-length" value="128"/>

  <!-- Allow common safe image formats -->
  <policy domain="coder" rights="read|write" pattern="PNG" />
  <policy domain="coder" rights="read|write" pattern="JPEG" />
  <policy domain="coder" rights="read|write" pattern="JPG" />
  <policy domain="coder" rights="read|write" pattern="GIF" />
  <policy domain="coder" rights="read|write" pattern="WEBP" />
  <policy domain="coder" rights="read|write" pattern="AVIF" />
  <policy domain="coder" rights="read|write" pattern="BMP" />
  <policy domain="coder" rights="read|write" pattern="TIFF" />
  <policy domain="coder" rights="read|write" pattern="ICO" />
</policymap>
